== global ==
deleting a device doesn't delete the dev_parents entries
make custom_graphs.php delete action a javascript
deleting a monitor doesn't work
page titles should reflect where you're at
"show summed" support in custom_graphs.php
redo custom_graphs.php to use unified add/edit code
fix move-up/move-down in views and graph-items
view page should reflect status information

== silfreed ==
fix everything so register_globals can be off
get auth w/ php sessions to work
  - include check for REMOTE_ADDR
  - set a standard for auth checks
    * think a combination of IsLoggedIn and check_auth(_level) would be good
    * what exactly are the different auth levels?

== balleman ==
graph templates
make events/responses work again
snmp cacher rewrite